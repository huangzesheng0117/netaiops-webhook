playbook_id: f5_device_reachability_down
match:
  vendor: f5
  alertname_regex: '^(up)$'
execution:
  readonly_only: true
  auto_execute_allowed: true
  max_commands: 5
  commands:
    - "tmsh show sys version"
    - "tmsh show sys failover"
    - "tmsh show sys performance system"
