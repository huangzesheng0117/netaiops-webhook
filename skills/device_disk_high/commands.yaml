skill_name: device_disk_high
version: v6.3.0
family: device_disk_high
risk_level: readonly
allowed_tools:
  - mcp_netmiko_run_show
  - prometheus_range_query
  - parser_parse_cli_output
allowed_capabilities:
  - show_file_systems
  - show_version_storage_hint
  - prometheus_range_query
  - parse_cli_output
platform_commands:
  cisco_nxos:
    show_file_systems:
      template: "show file systems"
      readonly: true
    show_version_storage_hint:
      template: "show version | include System image|bootflash|flash|image"
      readonly: true
    show_recent_logs:
      template: "show logging | include flash|bootflash|disk|filesystem|No space"
      readonly: true
  cisco_iosxe:
    show_file_systems:
      template: "show file systems"
      readonly: true
    show_version_storage_hint:
      template: "show version | include System image|bootflash|flash|image"
      readonly: true
    show_recent_logs:
      template: "show logging | include flash|bootflash|disk|filesystem|No space"
      readonly: true
  cisco_ios:
    show_file_systems:
      template: "show file systems"
      readonly: true
    show_version_storage_hint:
      template: "show version | include System image|bootflash|flash|image"
      readonly: true
    show_recent_logs:
      template: "show logging | include flash|bootflash|disk|filesystem|No space"
      readonly: true
  aci:
    show_file_systems:
      template: "show file systems"
      readonly: true
    show_version_storage_hint:
      template: "show version | include System image|bootflash|flash|image"
      readonly: true
    show_recent_logs:
      template: "show logging | include flash|bootflash|disk|filesystem|No space"
      readonly: true
command_limits:
  max_commands_per_request: 8
  max_extra_rounds: 0
readonly_only: true
forbidden_patterns:
  - "configure terminal"
  - "conf t"
  - "shutdown"
  - "no shutdown"
  - "clear counters"
  - "reload"
  - "write memory"
  - "copy running-config startup-config"
  - "delete"
  - "erase"
  - "debug"
